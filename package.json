{
  "name": "trening-app",
  "private": true,
  "version": "1.0.0",
  "description": "Sedmični raspored treninga s vježbama",
  "scripts": {
    "dev": "npm run dev --workspace=client",
    "build": "npm run build --workspace=client"
  },
  "devDependencies": {
    "@vercel/node": "^3.2.0",
    "concurrently": "^8.2.2"
  },
  "workspaces": [
    "client",
    "server"
  ]
}
